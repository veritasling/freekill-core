# 新月杀（FreeKill）动态皮肤系统技术白皮书 V10.8

## 一、项目概述与技术栈

### 1.1 游戏架构
新月杀采用**三层架构**：

| 层级 | 技术 | 职责 | 对应代码位置 |
|------|------|------|-------------|
| **系统层** | Qt6 (C++) | 文件系统、网络、多媒体解码（Video/GIF）、渲染引擎 | 游戏本体二进制 |
| **界面层** | QML (Qt Quick) | UI渲染、动画管理、用户交互逻辑 | `packages/freekill-core/Fk/` |
| **逻辑层** | Lua | 武将技能、卡牌效果、游戏规则运算 | `packages/*/pkg/` |
| **资源层** | 文件系统 | 图片、音频、视频资源 | `resource_pak/`, `packages/` |

**关键机制**：游戏启动时会将 `packages/freekill-core/` 内的 QML 文件**热重载**到内存，这意味着修改这些文件无需重新编译游戏本体即可生效。

### 1.2 渲染流程
当游戏需要显示一个武将时：
1. **数据层**：Lua 提供武将ID（如 `mobile__wangyuanji`）
2. **资源层**：`SkinBank` 类查找对应图片/视频路径
3. **UI层**：QML 组件（`GeneralCardItem` 或 `PhotoBase`）根据资源类型选择渲染方式（静态图/视频/GIF）

---

## 二、动态皮肤系统的技术实现

### 2.1 核心问题分析
**原始限制**：
- 游戏原生仅支持静态图片（`.jpg`）作为武将立绘
- 资源加载是同步阻塞的，大量文件检索会导致界面卡顿
- 视频切换时会出现黑屏（旧资源销毁到新资源解码之间的空白期）

**我们的解决方案**：
通过修改 `packages/freekill-core/Fk/` 下的三个核心文件，实现**非侵入式**的动态皮肤支持（无需修改游戏二进制或 Lua 逻辑）。

### 2.2 修改手段与文件分工

#### 文件 A：`Fk/Base/SkinBank.qml`（资源检索中枢）
**修改类型**：新增 API 函数  
**作用**：在原有静态图片检索基础上，增加动态资源发现能力。

**技术细节**：
- 新增 `getGeneralAnimationFiles(name)` 函数，按优先级检索 `name1.mp4/gif`（入场）、`name2.mp4/gif`（待机）、`name3.mp4/gif`（特殊）
- 遵循游戏原有资源覆盖逻辑：优先检查 `resource_pak/`（玩家自定义资源包），回退到 `packages/`（官方资源）
- 自动识别格式：优先 MP4（硬件解码），回退 GIF（软件解码）

#### 文件 B：`Fk/Components/LunarLTK/GeneralCardItem.qml`（武将一览）
**修改类型**：架构重构  
**作用**：选将界面、武将图鉴的动态皮肤播放。

**核心技术 - 双视频缓冲（Double Buffering）**：
为解决视频切换黑屏问题，我们实现了**双槽位交替渲染**：

```
[currentVideo] ← 当前显示（opacity: 1）
[nextVideo]    ← 预加载/下一帧（opacity: 0）
```

**状态机流转**：
1. **Entry（入场）**：播放 `1.mp4`，单次播放，结束自动进入 Idle
2. **Idle（待机）**：循环播放 `2.mp4`，无限循环
3. **Special（特殊）**：随机触发播放 `3.mp4`，结束后回到 Idle

**防闪黑机制**：
- **预渲染等待**：新视频在 `opacity: 0` 状态下先播放，等待 `position > 0`（确认首帧解码完成）后再执行 300ms 交叉淡化
- **背景兜底**：动画容器设置黑色背景，防止透明背景闪白
- **智能回退**：如果特殊动画结束但待机动画文件缺失，自动回退到入场动画或静态图，绝不黑屏

#### 文件 C：`Fk/Components/LunarLTK/PhotoBase.qml`（对局头像）
**修改类型**：逻辑同步  
**作用**：游戏对局中的右下角头像框动态皮肤。

**技术适配**：
- 复用与 `GeneralCardItem` 相同的双缓冲架构
- 针对头像框尺寸（131×174）优化裁剪模式（`PreserveAspectCrop`）
- 增加**视频卡滞检测**（Guardian Timer）：每秒检测视频进度，若连续 3 秒无变化（解码卡死），自动重启或切换状态

### 2.3 性能优化策略

**问题**：直接遍历所有资源包检索视频文件会导致列表滚动卡顿（文件系统 IO 阻塞）。

**解决方案**：
1. **白名单过滤**：只检索包名包含"动态"二字的资源包（如"手杀动态"、"十周年动态"），排除 90% 不可能包含视频的资源包
2. **函数级全局缓存**：利用 JavaScript 函数静态属性（`initializeAnimation.cache`）缓存每个武将的检索结果，同一会话内只检索一次
3. **延迟初始化**：使用 `Qt.callLater` 将初始化推迟到下一帧，避免阻塞 UI 线程

---

## 三、当前功能特性（V10.8）

### 3.1 已实现功能
✅ **三态动画支持**：入场（单次）→ 待机（循环）↔ 特殊（随机触发）  
✅ **双格式支持**：MP4（硬件加速，推荐）/ GIF（软件解码，兼容性好）  
✅ **无缝切换**：300ms 交叉淡化，无黑屏无闪白  
✅ **智能回退**：任何状态异常都自动降级到静态图，不死机不黑屏  
✅ **资源包兼容**：完美兼容现有 `resource_pak/` 结构，无需修改配置格式  
✅ **性能保障**：全局缓存 + 白名单过滤，列表滚动流畅无卡顿  

### 3.2 技术稳定性
- **内存管理**：`Component.onDestruction` 自动清理视频资源，无内存泄漏
- **错误处理**：视频解码失败、文件损坏、路径错误均自动捕获并回退静态图
- **长时间运行**：经过 2 小时+ 对局测试，无卡顿、无内存增长

---

## 四、资源包制作规范（给美化开发者）

### 4.1 文件结构
```
resource_pak/【你的包名】/
├── icon.png  (资源包图标)
└── packages/
    └── mobile/  (或 ol, heg 等官方包名)
        └── image/
            └── generals/
                ├── mobile__武将名1.mp4  (入场，播放一次)
                ├── mobile__武将名2.mp4  (待机，循环播放)
                └── mobile__武将名3.mp4  (特殊，随机触发)
```

### 4.2 命名规则
**文件名格式**：`前缀__武将名[1|2|3].mp4`

| 后缀 | 含义 | 播放逻辑 | 时长建议 |
|------|------|----------|----------|
| `1` | Entry（入场） | 单次播放，结束自动切换待机 | 2-4 秒 |
| `2` | Idle（待机） | 无限循环，需无缝衔接 | 3-8 秒（循环点需平滑） |
| `3` | Special（特殊） | 随机触发（平均每 8-15 秒），结束回待机 | 2-5 秒 |

**前缀对应**：
- `mobile__`：手杀武将
- `ol__`：OL 武将  
- `heg__`：国战武将
- `ten__`：十周年武将
- `zq__`：战棋武将

### 4.3 技术规格建议
- **视频编码**：H.264 (AVC)，Baseline 或 Main Profile，兼容性最佳
- **分辨率**：建议 540×960（竖屏）或 720×1280，与游戏立绘比例一致（约 9:16）（但是实在不行，只要是人物在视频中间的原视频即可，因为我做了自裁剪功能）
- **帧率**：30fps 即可，过高帧率增加功耗无肉眼收益
- **码率**：2-4 Mbps，平衡清晰度与解码性能
- **音频**：建议静音（`an` 参数），或保持与游戏 BGM 协调的音量

**FFmpeg 制作示例**：
```bash
# 入场动画（2秒，无音频）
ffmpeg -i input.mp4 -vf "scale=540:960:force_original_aspect_ratio=decrease,pad=540:960:(ow-iw)/2:(oh-ih)/2:black" -c:v libx264 -profile:v baseline -level 3.0 -an -t 2 output1.mp4

# 待机动画（4秒循环，需确保首尾帧一致）
ffmpeg -i input.mp4 -vf "scale=540:960:force_original_aspect_ratio=decrease,pad=540:960:(ow-iw)/2:(oh-ih)/2:black,loop=0" -c:v libx264 -an -t 4 output2.mp4
```

### 4.4 配置文件
在 `freekill.client.config.json` 中启用：
```json
{
  "enabledResourcePacks": ["【手杀动态】王元姬回廊", "【你的包名】"]
}
```

---

## 五、后续扩展方向

### 5.1 攻击/受伤动画（攻击动皮）
**可行性**：高  
**技术路径**：
- 新增状态 `attack`（`4.mp4`）和 `damage`（`5.mp4`）
- 在 `Photo.qml`（对局大头像）中添加信号监听：`onAttackStart`、`onDamageTaken`
- 需要配合 Lua 层发送信号，但 UI 层已具备视频切换能力

### 5.2 技能特效动画
**可行性**：中  
**技术路径**：
- 在 `GameRoom.qml` 中添加全屏透明视频层（`z: 999`）
- 监听技能触发信号，播放对应技能视频后淡出

### 5.3 动态背景（场景皮肤）
**可行性**：高  
**技术路径**：
- 修改 `RoomPage.qml` 或 `Dashboard.qml` 的背景组件
- 将静态 `Image` 替换为 `Video`，逻辑与武将动态皮肤完全一致

---

## 六、给审核者的技术摘要

**本次 PR 修改了三个文件，实现了一个完整的动态皮肤子系统：**

1. **零侵入性**：只修改 QML 界面层，不修改 C++ 核心或 Lua 逻辑，不影响游戏原有功能
2. **向后兼容**：无动态皮肤的武将自动显示静态图，无配置变更强制要求
3. **性能安全**：通过白名单和全局缓存确保列表滚动流畅，低端设备无压力
4. **鲁棒性**：任何视频错误（损坏、缺失、解码失败）都自动回退静态图，不死机

**代码质量**：
- 遵循 Qt/QML 最佳实践（属性绑定、信号槽、Timer 管理）
- 详细注释和调试日志（`>>>` 前缀）
- 内存安全（自动清理、无循环引用）

---

**文档版本**：V10.8  
**适用游戏版本**：FreeKill v0.5.19+  
**最后更新**：2026-02-06

---